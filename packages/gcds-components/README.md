# GCDS Components Package

This is a placeholder directory for the `gcds-components` fork.

**To set up for development:**

1. Remove this placeholder directory:
   ```bash
   rm -rf packages/gcds-components
   ```

2. Clone the actual fork repository:
   ```bash
   git clone https://github.com/gc-proto/gcds-components.git packages/gcds-components
   ```

3. Install dependencies:
   ```bash
   pnpm install
   ```

See [SETUP.md](../../SETUP.md) for complete setup instructions.
