{
  "name": "gcds.test.canada.ca",
  "version": "1.0.0",
  "description": "Eleventy development site for GC Design System components",
  "private": true,

  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --watch",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site",
    "copy-to-examples": "bash ../../scripts/copy-templates-to-examples.sh"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "gcds-components-repo": "workspace:*",
    "@gcds-core/css-shortcuts": "workspace:*"
  },
  "devDependencies": {
    "@11ty/eleventy-dev-server": "^1.0.4"
  }
}
